name: ci

on: push

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t vpn-stand:ci .
      - run: docker run --rm vpn-stand:ci wg --version
